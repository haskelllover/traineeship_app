<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Assign Professor</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h2>Assign Supervising Professor for <span th:text="${traineeship.position.title}"></span></h2>

    <form th:action="@{'/committee/assign-professor/' + ${traineeship.id}}" method="post">
        <div class="mb-3">
            <label for="professor" class="form-label">Choose Professor:</label>
            <select class="form-select" id="professor" name="professorId">
                <option th:each="prof : ${professors}"
                        th:value="${prof.id}"
                        th:text="${prof.name + ' (Load: ' + prof.load + ', Interests: ' + prof.interests + ')'}">
                </option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Assign</button>
    </form>
</div>
</body>
</html>
