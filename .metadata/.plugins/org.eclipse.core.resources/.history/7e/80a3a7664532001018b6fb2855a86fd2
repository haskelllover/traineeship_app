<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Monitor Evaluation</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h2>Monitor Evaluation for <span th:text="${traineeship.student.name}"></span></h2>

    <h5>Evaluation Reports</h5>
    <ul class="list-group mb-4">
        <li class="list-group-item" th:each="evaluation : ${evaluations}" th:text="${evaluation.summary}"></li>
    </ul>

    <form th:action="@{'/committee/complete/' + ${traineeship.id}}" method="post">
        <div class="mb-3">
            <label for="result" class="form-label">Final Decision:</label>
            <select name="result" id="result" class="form-select" required>
                <option value="PASS">Pass</option>
                <option value="FAIL">Fail</option>
            </select>
        </div>
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
</div>
</body>
</html>
