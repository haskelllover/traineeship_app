<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Application Status</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h3>Application Status</h3>
                <h5 th:text="'Student: ' + ${student.studentName}"></h5>
            </div>
            <div class="card-body">
                <div th:if="${isLooking}" class="alert alert-info">
                    <h5>Application Status: Submitted</h5>
                    <p>Your traineeship application has been received and is under review.</p>
                </div>
                <div th:unless="${isLooking}" class="alert alert-warning">
                    <h5>Application Status: Not Submitted</h5>
                    <p>You haven't applied for a traineeship yet.</p>
                    <a th:href="@{/student/apply}" class="btn btn-primary">Apply Now</a>
                </div>
                
                <div th:if="${position != null}" class="alert alert-success mt-3">
                    <h5>Assigned Traineeship</h5>
                    <p>Company: <span th:text="${position.companyName}"></span></p>
                    <p>Location: <span th:text="${position.location}"></span></p>
                    <p>Description: <span th:text="${position.description}"></span></p>
                    <a th:href="@{/student/logbook}" class="btn btn-success">Go to Logbook</a>
                </div>
                
                <div class="mt-3">
                    <a th:href="@{/student/dashboard}" class="btn btn-secondary">Back to Dashboard</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>