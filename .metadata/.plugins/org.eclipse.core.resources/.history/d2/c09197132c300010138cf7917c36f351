<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Application Status</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h3>Application Status</h3>
            </div>
            <div class="card-body">
                <div th:if="${isLooking}" class="alert alert-info">
                    You have applied for a traineeship. Status: Pending
                </div>
                <div th:unless="${isLooking}" class="alert alert-warning">
                    You haven't applied for a traineeship yet.
                </div>
                
                <div th:if="${position != null}" class="alert alert-success">
                    <h5>Assigned Traineeship</h5>
                    <p>Company: <span th:text="${position.companyName}"></span></p>
                    <p>Location: <span th:text="${position.location}"></span></p>
                    <p>Description: <span th:text="${position.description}"></span></p>
                </div>
                
                <a th:href="@{/student/dashboard}" class="btn btn-secondary">Back to Dashboard</a>
                <a th:href="@{/student/apply}" class="btn btn-primary">Apply for Traineeship</a>
            </div>
        </div>
    </div>
</body>
</html>