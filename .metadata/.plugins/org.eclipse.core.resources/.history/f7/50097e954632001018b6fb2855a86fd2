<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Match Positions</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h2>Available Positions for <span th:text="${student.name}"></span></h2>

    <form method="get" th:action="@{'/committee/students/' + ${student.id} + '/match'}" class="mb-3">
        <div class="row g-2">
            <div class="col-md-4">
                <input type="text" name="interest" class="form-control" placeholder="Interest">
            </div>
            <div class="col-md-4">
                <input type="text" name="location" class="form-control" placeholder="Preferred Location">
            </div>
            <div class="col-md-4">
                <button class="btn btn-primary w-100">Search</button>
            </div>
        </div>
    </form>

    <table class="table table-hover">
        <thead>
        <tr>
            <th>Title</th>
            <th>Company</th>
            <th>Location</th>
            <th>Required Skills</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="position : ${positions}">
            <td th:text="${position.title}"></td>
            <td th:text="${position.company.name}"></td>
            <td th:text="${position.location}"></td>
            <td th:text="${position.requiredSkills}"></td>
            <td>
                <a th:href="@{'/committee/assign/' + ${student.id} + '/' + ${position.id}}" class="btn btn-success btn-sm">Assign</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>
</body>
</html>
