<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>In-Progress Traineeships</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h2>Traineeships In Progress</h2>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>Student</th>
            <th>Position</th>
            <th>Company</th>
            <th>Professor</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="traineeship : ${traineeships}">
            <td th:text="${traineeship.student.name}"></td>
            <td th:text="${traineeship.position.title}"></td>
            <td th:text="${traineeship.position.company.name}"></td>
            <td th:text="${traineeship.professor?.name ?: 'Unassigned'}"></td>
            <td th:text="${traineeship.status}"></td>
            <td>
                <a th:href="@{'/committee/monitor/' + ${traineeship.id}}" class="btn btn-info btn-sm">Monitor</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>
</body>
</html>
