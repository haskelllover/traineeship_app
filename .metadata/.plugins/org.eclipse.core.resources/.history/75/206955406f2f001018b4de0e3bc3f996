<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <!-- Add your head content -->
</head>
<body>
    <div class="container">
        <h2>My Traineeship Logbook</h2>
        
        <div th:if="${errorMessage}" class="alert alert-danger" th:text="${errorMessage}"></div>
        <div th:if="${successMessage}" class="alert alert-success" th:text="${successMessage}"></div>
        
        <form th:action="@{/student/save-logbook}" th:object="${position}" method="post">
            <div class="form-group">
                <label>Logbook Entries</label>
                <textarea class="form-control" th:field="*{logbookEntries}" rows="10"></textarea>
            </div>
            
            <div class="form-group">
                <label>Hours Completed</label>
                <input type="number" class="form-control" th:field="*{hoursCompleted}">
            </div>
            
            <button type="submit" class="btn btn-primary">Save Logbook</button>
            <a th:href="@{/student/dashboard}" class="btn btn-secondary">Back to Dashboard</a>
        </form>
    </div>
</body>
</html>