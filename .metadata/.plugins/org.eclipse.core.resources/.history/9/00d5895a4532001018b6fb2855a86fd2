<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Assign Supervising Professor</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
    <h2>Assign Professor to <span th:text="${traineeship.title}">Traineeship</span></h2>

    <form th:action="@{'/committee/assign-professor/' + ${traineeship.id}}" method="post">
        <div class="mb-3">
            <label for="professor" class="form-label">Select Professor:</label>
            <select name="professorId" id="professor" class="form-select" required>
                <option th:each="prof : ${professors}" th:value="${prof.id}"
                        th:text="${prof.name + ' (Interests: ' + prof.interests + ', Load: ' + prof.load + ')'}">
                </option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Assign</button>
    </form>
</div>
</body>
</html>
